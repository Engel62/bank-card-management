markdown
# 🏦 Bank Card Management System

Система управления банковскими картами с JWT аутентификацией, шифрованием данных и ролевым доступом.

## 📋 Оглавление
- [Возможности](#возможности)
- [Технологии](#технологии)
- [Архитектура](#архитектура)
- [Запуск](#запуск)
- [API Документация](#api-документация)
- [Тестирование](#тестирование)
- [Безопасность](#безопасность)
- [Развертывание](#развертывание)

## 🚀 Возможности

### Для Администраторов (ROLE_ADMIN)
- ✅ Создание, блокировка, активация, удаление карт
- ✅ Управление пользователями (CRUD операции)
- ✅ Просмотр всех карт системы
- ✅ Фильтрация и пагинация карт

### Для Пользователей (ROLE_USER)
- ✅ Просмотр своих карт с пагинацией
- ✅ Блокировка своих карт
- ✅ Переводы между своими картами
- ✅ Просмотр баланса
- ✅ История транзакций

### Общие возможности
- ✅ JWT аутентификация
- ✅ Шифрование номеров карт (AES-256)
- ✅ Маскирование данных в ответах API
- ✅ Валидация входных данных
- ✅ Глобальная обработка ошибок
- ✅ Swagger/OpenAPI документация

## 🛠 Технологии

- **Java 17+** - основной язык программирования
- **Spring Boot 3.1.5** - фреймворк
- **Spring Security + JWT** - аутентификация и авторизация
- **Spring Data JPA** - работа с базой данных
- **PostgreSQL / H2** - базы данных
- **Liquibase** - миграции базы данных
- **Docker** - контейнеризация
- **OpenAPI 3** - документация API
- **JUnit 5 + Mockito** - тестирование

## 🏗 Архитектура
src/main/java/com/bank/card/
├── config/ # Конфигурации Spring
├── controller/ # REST контроллеры
├── dto/ # Data Transfer Objects
│ ├── request/ # DTO для запросов
│ └── response/ # DTO для ответов
├── entity/ # JPA сущности
├── exception/ # Кастомные исключения
├── repository/ # JPA репозитории
├── security/ # Spring Security
│ ├── jwt/ # JWT утилиты
│ └── service/ # UserDetailsService
└── service/ # Бизнес-логика

text

## 🚀 Запуск

### Предварительные требования
- Java 17+
- Maven 3.8+
- PostgreSQL (опционально)
- Docker (опционально)

### Способ 1: Локальный запуск с H2 (рекомендуется для разработки)


# 1. Клонировать репозиторий
```git clone <repository-url>
cd bank-card-management
```

# 2. Собрать проект
```mvn clean package```

# 3. Запустить приложение
java -jar target/card-management-1.0.0.jar

# Приложение будет доступно по адресу:
# http://localhost:8080
# Swagger UI: http://localhost:8080/swagger-ui.html
Способ 2: Запуск с PostgreSQL
bash
# 1. Установить и запустить PostgreSQL
# 2. Создать базу данных:
createdb bank_db
createuser bank_user -P  # пароль: bank_password

# 3. Запустить приложение с профилем prod
mvn spring-boot:run -Dspring-boot.run.profiles=prod
Способ 3: Docker Compose (самый простой)
bash
# Запустить все сервисы
docker-compose up --build

# Остановить
docker-compose down
📚 API Документация
Swagger UI
После запуска приложения документация доступна по адресу:

Swagger UI: http://localhost:8080/swagger-ui.html

OpenAPI JSON: http://localhost:8080/v3/api-docs

Основные endpoint'ы
Аутентификация
text
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
Карты
text
# Создать карту (Admin only)
POST /api/cards

# Получить мои карты
GET /api/cards/my?page=0&size=10

# Получить все карты (Admin only)
GET /api/cards

# Обновить статус карты
PATCH /api/cards/{id}/status?status=BLOCKED
Переводы
text
# Перевод между своими картами
POST /api/transfers/own

{
  "fromCardNumber": "4111111111111111",
  "toCardNumber": "4222222222222222",
  "amount": 100.00,
  "description": "Перевод за услуги"
}
Управление пользователями (Admin only)
text
# Получить всех пользователей
GET /api/admin/users

# Создать пользователя
POST /api/admin/users

# Обновить пользователя
PUT /api/admin/users/{id}

# Удалить пользователя
DELETE /api/admin/users/{id}
🧪 Тестирование
Запуск тестов
bash
# Все тесты
mvn test

# Только unit тесты
mvn test -Dtest="*ServiceTest"

# Интеграционные тесты
mvn test -Dtest="*ControllerTest"

# С генерацией отчета покрытия
mvn test jacoco:report
Предустановленные пользователи
Роль	Username	Password	Описание
ADMIN	admin	admin123	Полный доступ ко всем операциям
USER	user1	user123	Обычный пользователь
🔒 Безопасность
Шифрование данных
Номера карт: AES-256 шифрование

Пароли: BCrypt хеширование

JWT токены: HMAC-SHA256

Защита данных
Маскирование номеров карт в ответах API

Валидация всех входных данных

Защита от SQL-инъекций

CSRF защита отключена для REST API

Ролевая модель
ROLE_ADMIN: Полный доступ ко всем ресурсам

ROLE_USER: Доступ только к своим данным

🐳 Развертывание
Docker
dockerfile
FROM openjdk:17-jdk-slim
COPY target/card-management-*.jar app.jar
ENTRYPOINT ["java", "-jar", "app.jar"]
Docker Compose
yaml
version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: bank_db
      POSTGRES_USER: bank_user
      POSTGRES_PASSWORD: bank_password
  
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - postgres
Конфигурация окружения
yaml
# application-prod.yml
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/bank_db
    username: ${DB_USER:bank_user}
    password: ${DB_PASSWORD:bank_password}

app:
  security:
    jwt:
      secret-key: ${JWT_SECRET:your-secret-key}
📊 Миграции базы данных
Проект использует Liquibase для управления миграциями. Миграции находятся в:

text
src/main/resources/db/migration/
Создание новой миграции
bash
# Генерация changelog
mvn liquibase:diff

# Применение миграций
mvn liquibase:update


